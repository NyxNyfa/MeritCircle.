{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm prisma:generate && pnpm --filter @merit-circle/backend run build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @merit-circle/backend run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
